# Booky share assets

Canonical promo graphics from [lifewithchevy/booky-assets](https://github.com/lifewithchevy/booky-assets).

- `weavingshaw.jpg` — local cover for PNG export / offline preview (no CORS)
- Rendered via `../promo-render.js` (same SVG as booky-assets `index.html`)

Per new author day: add `{slug}.jpg` here and link in `words.json`.
